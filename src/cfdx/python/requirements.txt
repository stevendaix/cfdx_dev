# CFDX I/O Adapters — Python dependencies
#
# Core dependencies (always required for I/O pipeline)
h5py>=3.16
numpy>=2.2
meshio>=5.3.5

# Optional dependencies — install only for extended solver support
#
# Fluent .cas.h5 (HDF5) reader — no Fluent license required:
#   pip install ansys-fluent-core
ansys-fluent-core>=0.42

# SU2 adapter — reads SU2 mesh and config files:
#   (no extra dependencies — pure Python parser)

# STAR-CCM+ adapter — reads binary .sim via ASCII string extraction:
#   (no extra dependencies)

# Code_Saturne adapter — reads XML + Python case files:
#   (no extra dependencies)
